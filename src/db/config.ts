const config = {
  /* don't expose password or any sensitive info, done only for demo */
  host: 'localhost',
  port: 3306,
  user: 'root',
  password: '1234',
  database: 'project'
};

export { config };
